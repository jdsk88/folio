import React from 'react';
// import { useStyles } from "./styles"

export const Greeter = (props) => {

    // const classes = useStyles();
    return <div>Hello {props.user} aa {props.what} </div>;
}
